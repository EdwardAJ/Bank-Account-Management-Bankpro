stages:
  - build
  - deploy

lint-and-test:
  stage: build
  tags:
    - tubes2wbd
  script:
    - 'sudo npm install'
    - 'sudo chown ubuntu node_modules'
    - 'sudo chgrp ubuntu node_modules'
    - 'eslint src'
    - 'sudo npm run test'
    - 'whoami'

bankpro-deploy:
  stage: deploy
  tags:
    - tubes2wbd
  script:
    - bash aws-command.sh
